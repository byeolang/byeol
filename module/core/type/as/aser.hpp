/// @file
#pragma once

#include "core/type/as/asable.hpp"

namespace by {
    /// @ingroup core
    /// @brief Base type converter
    /// @details Base implementation of type conversion interface.
    class _nout aser: public instance, public asable {
        BY(ME(aser, instance))
    };
}
